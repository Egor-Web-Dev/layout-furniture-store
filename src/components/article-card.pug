article(class=`article ${className ? className : ""}`)
  .article__img
    picture.article__picture
      source(srcset=`../assets/img/${articleImg}-large.webp` media="(min-width: 1400px)" type="image/webp")
      source(srcset=`../assets/img/${articleImg}.webp` type="image/webp")
      img(src=`../assets/img/${articleImg}-large.webp` loading="lazy" alt="")

  .article__data
    h5.article__title= articleTitle
    button.btn.article__btn Читать
